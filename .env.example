# B-Plus POS Environment Configuration
# Copy this file to .env and update with your values

# Database Configuration (Required)
# Choose one of these based on your setup:

# For Railway (recommended for production)
# Use the POSTGRES_URL from your Railway PostgreSQL service
# DATABASE_URL=postgresql://postgres:password@host:port/database

# For Neon (serverless PostgreSQL)
# DATABASE_URL=postgresql://username:password@ep-xxx.us-east-1.aws.neon.tech/neondb

# For local development
DATABASE_URL=postgresql://postgres:password@localhost:5432/bplus_pos

# Alternative environment variable (used by some platforms)
POSTGRES_URL=

# Session Security (Required)
# Generate a secure random string (minimum 32 characters)
SESSION_SECRET=change-this-to-a-secure-random-string-for-production-use

# Application Environment
NODE_ENV=development
PORT=5000

# Production Notes:
# 1. Never commit the .env file to version control
# 2. Use your platform's environment variable settings for production
# 3. Generate a strong SESSION_SECRET for production
# 4. Set NODE_ENV=production in production environments
WOOCOMMERCE_CONSUMER_SECRET=cs_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Email Configuration (Optional - for receipts)
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx